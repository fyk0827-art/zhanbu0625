server:
  port: 8892

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/0625zhanbu?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: 0625zhanbu
    password: 0625zhanbu
  jpa:
    hibernate:
      ddl-auto: update

payment:
  mode: paypal
  order-amount: 2990
  frontend-url: https://divinlove.com
  base-url: https://divinlove.com
  paypal:
    client-id: ${PAYPAL_CLIENT_ID}
    secret: ${PAYPAL_SECRET}
    mode: ${PAYPAL_MODE:live}
